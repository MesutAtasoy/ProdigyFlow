name: ProdigyFlow CI

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  
jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore ProdigyFlow.sln

      - name: Build solution
        run: dotnet build ProdigyFlow.sln --no-restore --configuration Release
        
      - name: Get PR diff
        id: get_diff
        run: |
            if [ "${{ github.event_name }}" == "pull_request" ]; then
              curl -H "Accept: application/vnd.github.v3.diff" \
                   ${{ github.event.pull_request.url }} \
                   -o pr_diff.txt
            else
              git fetch origin main
              git diff origin/main HEAD > pr_diff.txt
            fi        

      - name: Run AI Test Prioritization
        env:
            GOOGLE_GEMINI_API_KEY: ${{ secrets.GOOGLE_GEMINI_API_KEY }}
        run: dotnet run --project src/ProdigyFlow.AI pr_diff.txt
      
      - name: Run prioritized tests
        run: |
          PRIORITIZED_TESTS_FILE="src/ProdigyFlow.AI/bin/Release/net8.0/prioritized_tests.txt"
          echo "Running prioritized tests from $PRIORITIZED_TESTS_FILE"
          if [ -f "$PRIORITIZED_TESTS_FILE" ]; then
            while read testName; do
              echo "Running test: $testName"
              dotnet test ProdigyFlow.sln --no-build --configuration Release --filter "FullyQualifiedName~$testName"
            done < "$PRIORITIZED_TESTS_FILE"
          else
            echo "No prioritized tests found. Running all tests."
            dotnet test ProdigyFlow.sln --no-build --configuration Release
          fi
          
      - name: Post AI Insights as PR Comment
        if: github.event_name == 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          SUMMARY_FILE="src/ProdigyFlow.AI/bin/Release/net8.0/ai_summary.txt"
          RISK_FILE="src/ProdigyFlow.AI/bin/Release/net8.0/ai_risk.txt"
          TESTS_FILE="src/ProdigyFlow.AI/bin/Release/net8.0/prioritized_tests.txt"

          SUMMARY=$(if [ -f "$SUMMARY_FILE" ]; then cat "$SUMMARY_FILE"; else echo "No summary generated."; fi)
          RISK=$(if [ -f "$RISK_FILE" ]; then cat "$RISK_FILE"; else echo "Unknown"; fi)
          TESTS=$(if [ -f "$TESTS_FILE" ]; then cat "$TESTS_FILE"; else echo "No tests prioritized."; fi)

          COMMENT_BODY=$(jq -n \
            --arg summary "$SUMMARY" \
            --arg risk "$RISK" \
            --arg tests "$TESTS" \
            '{
              body: "## 🤖 ProdigyFlow AI Insights\n\n**Summary:**\n\($summary)\n\n**Risk Score:** \($risk)\n\n**Prioritized Tests:**\n```\n\($tests)\n```\n\n_Posted automatically by ProdigyFlow AI Assistant._"
            }'
          )

          echo "Posting comment to PR..."
          curl -s -H "Authorization: token $GITHUB_TOKEN" \
               -H "Content-Type: application/json" \
               -d "$COMMENT_BODY" \
               "${{ github.event.pull_request.comments_url }}" 